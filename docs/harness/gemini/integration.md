# gemini 集成

使用 GEMINI_CLI_SYSTEM_DEFAULTS_PATH 指向合并后的独立配置，保留原系统默认值，追加生命周期 hooks；上层配置仍优先。Notification ToolPermission 映射等待关注，AfterAgent 映射完成。

来源、更新步骤与限制见 [Orca 对照表](../ORCA-UPSTREAM.md)。本轮仅构建，未运行测试或 CLI 交互验证。
